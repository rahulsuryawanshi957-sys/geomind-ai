# Dev/test-only dependencies -- NOT installed on Render (render.yaml's
# buildCommand only installs requirements.txt, deliberately, so the
# production deploy stays lean). Install locally/CI with:
#   pip install -r requirements.txt -r requirements-dev.txt
# then run tests from the backend/ directory with:
#   pytest
pytest==8.3.3
