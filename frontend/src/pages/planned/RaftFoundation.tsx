import ComingSoon from '../../components/ComingSoon'
export default function RaftFoundation() {
  return <ComingSoon
    title="Raft Foundation Design"
    description="Bearing capacity, settlement, and load distribution for combined/raft footings, wired to the same borehole data as the other foundation modules."
    plannedFeatures={['Rigid & flexible raft bearing checks', 'Differential settlement estimate', 'Contact pressure distribution', 'IS 2950 design checks']}
  />
}
